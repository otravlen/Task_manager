namespace Task_manager.Enum
{
    
        public enum StatusCode
        {
            OK = 200,
            InternalServerError = 500,
            TaskIsHasAlready = 1,
            TaskNotFound = 2
    }
    
}
